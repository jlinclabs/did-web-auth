<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    {{!-- <link rel="icon" href="/favicon/favicon.ico" /> --}}
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, viewport-fit=cover"/>
    {{!-- <meta name="theme-color" content="#111119" /> --}}
    <meta name="description" content="DID Auth Demo" />
    <title>DID Auth Demo</title>
    {{!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous"> --}}
    <link href="/index.css" rel="stylesheet">
  </head>
  <body class="" data-bs-theme="light">
    <div class="container-lg">
      {{!-- <nav class="navbar bg-body-secondary">
        <div class="container-lg">
          <img src="/favicon/favicon-32x32.png">
          <a class="navbar-brand" href="/">Wine About It!</a>

          <ul style="flex-grow: 1; display: flex; flex-direction: row; gap: 10px;" class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="/cart">Cart</a>
            </li>
          </ul>

          <div>
            {{#if user}}
              <div>
                <div class="dropdown">
                  <button class="btn dropdown-toggle" type="button" id="dropdownMenu2" data-bs-toggle="dropdown" aria-expanded="false">
                    <span>{{profile.name}}</span>
                    {{#if profile.avatar}}
                      {{> avatar src=profile.avatar alt=profile.name}}
                    {{/if}}
                  </button>
                  <ul class="dropdown-menu" aria-labelledby="dropdownMenu2">
                    <li>
                      <a href={{user.urls.account}} class="dropdown-item" type="button">Account</a>
                    </li>
                    <li>
                      <a href="/logout" class="dropdown-item" type="button">Logout</a>
                    </li>
                  </ul>
                </div>
              </div>
            {{else}}
              {{#> datapalButton href="/login" }}
                Login via DataPal
              {{/ datapalButton }}
            {{/if}}
          </div>
        </div>
      </nav> --}}

      <main>{{{body}}}</main>
    </div>

    {{!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script> --}}
  </body>
</html>
