<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <meta name="description" content="DID Auth POC" />
    <title>DID Auth POC</title>
    <link href="/index.css" rel="stylesheet">
  </head>
  <body>
    <div class="">
      <div
        class="p-2 grid gap-2 grid-cols-3 items-center"
        style="background-color: {{appColor}};"
      >
        <a class="text-blue-500 hover:text-blue-800" href="/">
          {{host}}
        </a>
        <a class="justify-self-center text-xl text-blue-500 hover:text-blue-800" href="/">
          DID Auth POC
        </a>
        <div class="justify-self-end">
          {{#if user}}
            <span class="text-gray-700 text-sm font-bold">{{usernameToEmail user.username}}</span>
            {{> signoutForm }}
          {{else}}

          {{/if}}
        </div>
      </div>
      <main>{{{body}}}</main>
    </div>
  </body>
</html>
